# ===================== CONFIGURAÇÃO DE PRODUÇÃO =====================

# ===================== CONFIGURAÇÃO DE BANCO DE DADOS =====================
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# ===================== BANCO REFLEXO =====================
spring.datasource.secondary.url=${DB_SECONDARY_URL}
spring.datasource.secondary.username=${DB_SECONDARY_USERNAME}
spring.datasource.secondary.password=${DB_SECONDARY_PASSWORD}
spring.datasource.secondary.driver-class-name=org.postgresql.Driver

# ===================== CONFIGURAÇÃO JPA PRODUÇÃO =====================
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# ===================== CONFIGURAÇÃO FLYWAY (AMBIENTE PRODUÇÃO) =====================
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.clean-disabled=true
spring.flyway.out-of-order=true
spring.flyway.validate-on-migrate=false
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=0
spring.flyway.ignore-migration-patterns=*:missing,*:future,*:pending

# ===================== CONFIGURAÇÃO DE LOGGING =====================
logging.level.root=INFO
logging.level.com.z7design.fleet_manager=INFO
logging.level.org.springframework.boot=INFO
logging.level.org.springframework.web=INFO
logging.level.org.hibernate=WARN


# ===================== CONFIGURAÇÃO DE SEGURANÇA =====================
server.ssl.enabled=false
# server.ssl.key-store=${SSL_KEYSTORE_PATH}
# server.ssl.key-store-password=${SSL_KEYSTORE_PASSWORD}
# server.ssl.key-store-type=PKCS12

# ===================== CONFIGURAÇÃO JWT =====================
jwt.secret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
jwt.expiration=${JWT_EXPIRATION:604800000}
jwt.header=Authorization
jwt.prefix=Bearer
jwt.refreshTokenExpiration=${JWT_REFRESH_EXPIRATION:604800000}

# ===================== CONFIGURAÇÃO ACTUATOR =====================
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized

# ===================== CONFIGURAÇÃO DE SERVIDOR =====================
server.port=8083
server.address=0.0.0.0
# IMPORTANTE: Respeitar headers X-Forwarded-Proto do Cloudflare/Traefik/Nginx
# Sem isso, o Spring pode tentar redirecionar para HTTPS causando ERR_TOO_MANY_REDIRECTS
server.forward-headers-strategy=framework
spring.main.allow-circular-references=true

# ===================== CONFIGURAÇÃO DE PERFORMANCE =====================
server.tomcat.max-threads=200
server.tomcat.min-spare-threads=20
server.tomcat.max-connections=10000
server.tomcat.accept-count=100

# ===================== CONFIGURAÇÃO DE EMAIL =====================
# Email: securedguard@z7design.com.br
# Senha: sg@2025promover
# Servidor: mail.z7design.com.br:465 (SSL/TLS)
spring.mail.host=${MAIL_HOST:186.209.113.105}
spring.mail.port=${MAIL_PORT:465}
spring.mail.username=${MAIL_USERNAME:securedguard@z7design.com.br}
spring.mail.password=${MAIL_PASSWORD:D8rKeqSFZfaS$(y7}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=false
spring.mail.properties.mail.smtp.starttls.required=false
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.ssl.trust=mail.z7design.com.br
spring.mail.properties.mail.smtp.from=FluxBus <${MAIL_FROM:securedguard@z7design.com.br}>

# ===================== CONFIGURAÇÃO RECAPTCHA =====================
recaptcha.secret.key=${RECAPTCHA_SECRET_KEY:default_recaptcha_key_for_production}
recaptcha.verify.url=https://www.google.com/recaptcha/api/siteverify

# ===================== CONFIGURAÇÃO N8N WHATSAPP =====================
n8n.enabled=${N8N_ENABLED:false}
n8n.base-url=${N8N_BASE_URL}
n8n.whatsapp.provider=${N8N_WHATSAPP_PROVIDER:wppconnect}
n8n.whatsapp.session-name=${N8N_SESSION_NAME:fluxbus}
n8n.whatsapp.auto-start=${N8N_AUTO_START:false} 