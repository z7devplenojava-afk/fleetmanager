# ===================== CONFIGURAÇÃO DE TESTE =====================

# ===================== CONFIGURAÇÃO DE BANCO DE DADOS =====================
spring.datasource.url=jdbc:postgresql://localhost:5432/fleetmanager_test
spring.datasource.username=postgres
spring.datasource.password=1234567
spring.datasource.driver-class-name=org.postgresql.Driver


# ===================== CONFIGURAÇÃO JPA TESTE =====================
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ===================== CONFIGURAÇÃO FLYWAY =====================
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.clean-disabled=false
spring.flyway.out-of-order=true
spring.flyway.validate-on-migrate=false
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=0

# ===================== CONFIGURAÇÃO DE LOGGING =====================
logging.level.root=WARN
logging.level.com.z7design.secured_guard=INFO
logging.level.org.springframework.test=DEBUG

# ===================== CONFIGURAÇÃO DE DADOS DE TESTE =====================
test.data.enabled=true
test.data.users.count=5
test.data.employees.count=10 

# ===================== CONFIGURAÇÃO DE SERVIDOR =====================
server.port=8081

# ===================== CONFIGURAÇÃO JWT =====================
jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
jwt.expiration=604800000
jwt.header=Authorization
jwt.prefix=Bearer
jwt.refreshTokenExpiration=604800000

# ===================== CONFIGURAÇÃO DE EMAIL PARA TESTE =====================
# Configuração dummy para evitar erros de inicialização
spring.mail.host=localhost
spring.mail.port=25
spring.mail.username=test@localhost
spring.mail.password=test
spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.starttls.enable=false

# ===================== CONFIGURAÇÃO DE SINCRONIZAÇÃO =====================
# Habilitar sincronização automática com banco secundário
app.database.sync.enabled=true
app.database.sync.interval=300000
app.database.sync.tables=users,roles,permissions,employees,departments,positions,units,clients,contracts,messages,notifications,equipments,vehicles,fuel_records,inventory_items,financial_transactions,orders_of_service,job_vacancies,job_candidates

# ===================== CONFIGURAÇÃO DE SINCRONIZAÇÃO =====================
# Habilitar sincronização automática com banco secundário
app.database.sync.enabled=true
app.database.sync.interval=300000
app.database.sync.tables=users,roles,permissions,employees,departments,positions,units,clients,contracts,messages,notifications,equipments,vehicles,fuel_records,inventory_items,financial_transactions,orders_of_service,job_vacancies,job_candidates