# =====================================================
# FLUXBUS - PERFIL CI (Integração Contínua)
# =====================================================

# ===== APLICAÇÃO =====
spring.application.name=FluxBus CI
server.port=8080
server.address=0.0.0.0

# ===== BANCO DE DADOS =====
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/fluxbus_db_ci}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:fluxbus_user_ci}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:4KaCiJc6an@7sgbdcid2026}
spring.datasource.driver-class-name=org.postgresql.Driver
# Configurações de inicialização do datasource
spring.datasource.hikari.initialization-fail-timeout=-1
spring.datasource.hikari.connection-test-query=SELECT 1
spring.datasource.hikari.validation-timeout=5000

# ===== JPA/HIBERNATE =====
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.use_sql_comments=false
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
# Configurações para melhorar inicialização
# Removido: hibernate.connection.provider_disables_autocommit=true (causa conflito com HikariCP)
spring.jpa.open-in-view=false
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=600000
spring.datasource.hikari.leak-detection-threshold=60000

# ===== FLYWAY =====
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
# Incluir migrations padrão e migrations específicas do CI
spring.flyway.locations=classpath:db/migration,classpath:db/migration-ci
spring.flyway.baseline-version=0
spring.flyway.validate-on-migrate=false
# Não continuar em caso de erro - melhor falhar explicitamente
spring.flyway.continue-on-error=false
spring.flyway.clean-disabled=true
# Configurações adicionais para garantir execução das migrations
# Permitir migrações fora de ordem para recuperar de migrações puladas
spring.flyway.out-of-order=true
spring.flyway.ignore-missing-migrations=true
spring.flyway.ignore-future-migrations=true
spring.flyway.ignore-migration-patterns=*:missing,*:future,*:pending
# Configurações para resolver problemas de lock do PostgreSQL
# Nota: Se o Flyway estiver travado em locks, execute no banco:
# SELECT pg_advisory_unlock_all();
# Timeout aumentado para migrations longas
spring.flyway.connect-retries=10
spring.flyway.connect-retries-interval=10

# ===== UPLOADS =====
upload.dir=${UPLOAD_DIR:/var/www/fluxbus/uploads}
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# ===== LOGS =====
logging.level.root=INFO
logging.level.com.z7design.fleet_manager=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.hibernate=INFO
logging.level.org.flywaydb=INFO
logging.level.org.flywaydb.core.internal.command.DbMigrate=DEBUG
logging.file.name=${LOGGING_FILE_PATH:/var/www/fluxbus/logs/application.log}
logging.file.max-size=10MB
logging.file.max-history=30
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# ===== CORS =====
cors.allowed-origins=https://ci.fluxbus.com.br,https://dev.fluxbus.com.br,https://fluxbus.com.br,http://localhost:3000,http://localhost:5173
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS,PATCH
cors.allowed-headers=*
cors.allow-credentials=true

# ===== JWT =====
# HS512 requires at least 64 bytes (512 bits) - this key is 80 bytes for extra security
jwt.secret=${JWT_SECRET:jwt_secret2026}
jwt.expiration=${JWT_EXPIRATION:604800000}
jwt.refresh-expiration=${JWT_REFRESH_EXPIRATION:2592000000}

# ===== SEGURANÇA =====
spring.security.filter.dispatcher-types=request,forward,error
spring.main.allow-circular-references=true

# ===== SERVIDOR =====
# IMPORTANTE: Respeitar headers X-Forwarded-Proto do Cloudflare/Traefik
# Sem isso, o Spring pode tentar redirecionar para HTTPS causando ERR_TOO_MANY_REDIRECTS
server.forward-headers-strategy=framework
server.compression.enabled=true
server.compression.mime-types=text/html,text/xml,text/plain,text/css,text/javascript,application/javascript,application/json
server.compression.min-response-size=1024
server.error.include-message=always
server.error.include-binding-errors=always

# ===== JACKSON =====
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=America/Sao_Paulo

# ===== DEVTOOLS DESABILITADO =====
spring.devtools.restart.enabled=false
spring.devtools.livereload.enabled=false

# ===== ACTUATOR (HEALTH CHECK) =====
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized
management.health.diskspace.enabled=true
management.health.db.enabled=true

# ===== TIMEZONE =====
spring.jpa.properties.hibernate.jdbc.time_zone=America/Sao_Paulo

# ===== WHATSAPP PROVIDER =====
whatsapp.provider=evolution

# ===== EVOLUTION API (WhatsApp) =====
evolution.api.url=http://evolution-api-ci:8080
evolution.api.key=${EVOLUTION_API_KEY:-B6D711FCDE4D4FD5936544120E713976}
evolution.api.instance=fluxbus

# ===== BAILEYS REST API (WhatsApp Alternative) =====
# No CI, usar o serviço whatsapp-service-ci que está rodando na porta 3333
# O serviço está definido no docker-compose.ci.yml como whatsapp-service-ci
# ⚠️ MODO DE TESTE: Habilitado para permitir uso do Baileys (não oficial)
whatsapp.test.mode=true
whatsapp.max.messages.per.day=0
baileys.rest.url=${BAILEYS_REST_URL:http://whatsapp-service-ci:3333}
baileys.enabled=${BAILEYS_ENABLED:true}
baileys.rest.token=${BAILEYS_REST_TOKEN:}
baileys.rest.instance.key=${BAILEYS_INSTANCE_KEY:fluxbus_ci}

# ===== CONFIGURAÇÃO DE EMAIL CI =====
# Email: securedguard@z7design.com.br
# Senha: D8rKeqSFZfaS$(y7
# Servidor: mail.z7design.com.br:465 (SSL/TLS)
# NOTA: A senha padrão acima é apenas um fallback. O valor real deve vir de MAIL_PASSWORD no .env
spring.mail.host=${MAIL_HOST:186.209.113.105}
spring.mail.port=${MAIL_PORT:465}
spring.mail.username=${MAIL_USERNAME:securedguard@z7design.com.br}
# IMPORTANTE: A senha deve vir da variável de ambiente MAIL_PASSWORD
# Se não estiver definida, o Spring Boot tentará usar o valor padrão (que pode ter caracteres especiais problemáticos)
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_STARTTLS_ENABLE:false}
spring.mail.properties.mail.smtp.starttls.required=${MAIL_STARTTLS_REQUIRED:false}
spring.mail.properties.mail.smtp.ssl.enable=${MAIL_SSL_ENABLE:true}
spring.mail.properties.mail.smtp.ssl.trust=*
spring.mail.properties.mail.smtp.ssl.protocols=TLSv1.2,TLSv1.3
# SocketFactory apenas para porta 465 (SSL direto)
spring.mail.properties.mail.smtp.socketFactory.class=${MAIL_SOCKET_FACTORY:javax.net.ssl.SSLSocketFactory}
spring.mail.properties.mail.smtp.socketFactory.port=${MAIL_SOCKET_FACTORY_PORT:465}
spring.mail.properties.mail.smtp.socketFactory.fallback=false
# Timeouts reduzidos para evitar travamento do servidor (10 segundos em vez de 30)
spring.mail.properties.mail.smtp.connectiontimeout=${MAIL_CONNECTION_TIMEOUT:10000}
spring.mail.properties.mail.smtp.timeout=${MAIL_TIMEOUT:10000}
spring.mail.properties.mail.smtp.writetimeout=${MAIL_WRITE_TIMEOUT:10000}
spring.mail.properties.mail.debug=${MAIL_DEBUG:false}
spring.mail.properties.mail.smtp.from=FluxBus <${MAIL_USERNAME:securedguard@z7design.com.br}>

# ===== REDIS =====
spring.data.redis.host=${SPRING_REDIS_HOST:redis-ci}
spring.data.redis.port=${SPRING_REDIS_PORT:6379}
spring.data.redis.password=${SPRING_REDIS_PASSWORD:redis_ci_2026}
spring.data.redis.timeout=2000ms
spring.data.redis.lettuce.pool.max-active=8
spring.data.redis.lettuce.pool.max-idle=8
spring.data.redis.lettuce.pool.min-idle=0

# ===== CACHE CONFIGURATION =====
# Usar Redis para cache distribuído
spring.cache.type=redis
spring.cache.redis.time-to-live=1800
spring.cache.redis.cache-null-values=false
