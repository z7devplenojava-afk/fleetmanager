/*
package br.com.fleetmanager.service;

import lombok.extern.slf4j.Slf4j;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.messaging.simp.SimpMessagingTemplate;
import org.springframework.stereotype.Service;
import br.com.fleetmanager.model.Notification;
import br.com.fleetmanager.model.User;
import br.com.fleetmanager.repository.NotificationRepository;
import br.com.fleetmanager.repository.UserRepository;
import br.com.fleetmanager.dto.NotificationDTO;
import br.com.fleetmanager.enums.NotificationType;
import br.com.fleetmanager.enums.NotificationPriority;
import br.com.fleetmanager.enums.NotificationStatus;
import br.com.fleetmanager.service.EmailService;
import br.com.fleetmanager.service.WhatsAppService;

import java.time.LocalDateTime;
import java.util.List;
import java.util.UUID;
import java.util.stream.Collectors;

@Service
@Slf4j
public class RealTimeNotificationService {

    @Autowired
    private SimpMessagingTemplate messagingTemplate;
    
    @Autowired
    private NotificationRepository notificationRepository;
    
    @Autowired
    private UserRepository userRepository;
    
    @Autowired
    private EmailService emailService;
    
    @Autowired
    private WhatsAppService whatsAppService;

    // Métodos comentados temporariamente para evitar erros de compilação
    // Serão reativados após correção das dependências
    
}
*/