version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: postgres-dev
    environment:
      POSTGRES_DB: ${DB_NAME:-fluxbus_dev}
      POSTGRES_USER: ${DB_USER:-fluxbus}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-fluxbus123}
    volumes:
      - postgres_dev_data:/var/lib/postgresql/data
    networks:
      - fluxbus_dev
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-fluxbus} -d ${DB_NAME:-fluxbus_dev}"]
      interval: 30s
      timeout: 10s
      retries: 5
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: redis-dev
    command: redis-server --requirepass ${REDIS_PASSWORD:-redispassword}
    volumes:
      - redis_dev_data:/data
    networks:
      - fluxbus_dev
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD:-redispassword}", "ping"]
      interval: 30s
      timeout: 10s
      retries: 5
    restart: unless-stopped

  whatsapp:
    image: ghcr.io/${GITHUB_REPOSITORY_OWNER}/fluxbus-whatsapp:dev
    container_name: whatsapp-dev
    depends_on:
      - redis
    environment:
      - REDIS_URL=redis://:${REDIS_PASSWORD:-redispassword}@redis-dev:6379
    volumes:
      - whatsapp_dev_data:/app/.wwebjs_auth
      - whatsapp_dev_cache:/app/.wwebjs_cache
    networks:
      - fluxbus_dev
    restart: unless-stopped

  backend:
    image: ghcr.io/${GITHUB_REPOSITORY_OWNER}/fluxbus-backend:dev
    container_name: backend-dev
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      - SPRING_PROFILES_ACTIVE=dev
      - DB_URL=jdbc:postgresql://postgres-dev:5432/${DB_NAME:-fluxbus_dev}
      - DB_USERNAME=${DB_USER:-fluxbus}
      - DB_PASSWORD=${DB_PASSWORD:-fluxbus123}
      - REDIS_HOST=redis-dev
      - REDIS_PORT=6379
      - REDIS_PASSWORD=${REDIS_PASSWORD:-redispassword}
      - JWT_SECRET=${JWT_SECRET:-404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
    networks:
      - fluxbus_dev
    expose:
      - 8083
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:8083/api/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 120s
    restart: unless-stopped

  frontend:
    image: ghcr.io/${GITHUB_REPOSITORY_OWNER}/fluxbus-frontend:dev
    container_name: frontend-dev
    depends_on:
      - backend
    networks:
      - fluxbus_dev
    expose:
      - 80
    restart: unless-stopped

volumes:
  postgres_dev_data:
  redis_dev_data:
  whatsapp_dev_data:
  whatsapp_dev_cache:

networks:
  fluxbus_dev:
    driver: bridge
