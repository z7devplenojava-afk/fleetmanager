version: '3.8'

# ============================================
# FLUXBUS - AMBIENTE LOCAL
# Nomenclatura: fluxbus-local-*
# Uso: docker-compose -f docker-compose.local.yml up -d
# ============================================

services:
  # ==========================================
  # POSTGRESQL - Banco de Dados Local
  # ==========================================
  postgres:
    image: postgres:15-alpine
    container_name: fluxbus-local-db
    hostname: fluxbus-local-db
    environment:
      POSTGRES_DB: fluxbus_db_local
      POSTGRES_USER: dev_user
      POSTGRES_PASSWORD: dev_pass
    ports:
      - "5432:5432"
    volumes:
      - fluxbus-local-db-data:/var/lib/postgresql/data
      - ./seeds/schema:/docker-entrypoint-initdb.d/01-schema:ro
      - ./seeds/common:/docker-entrypoint-initdb.d/02-common:ro
      - ./seeds/local:/docker-entrypoint-initdb.d/03-local:ro
    networks:
      - fluxbus-local-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dev_user -d fluxbus_db_local"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  # ==========================================
  # REDIS - Cache Local
  # ==========================================
  redis:
    image: redis:7-alpine
    container_name: fluxbus-local-redis
    hostname: fluxbus-local-redis
    ports:
      - "6379:6379"
    volumes:
      - fluxbus-local-redis-data:/data
    networks:
      - fluxbus-local-network
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  # ==========================================
  # MINIO - Armazenamento S3 Local
  # ==========================================
  minio:
    image: minio/minio:latest
    container_name: fluxbus-local-minio
    hostname: fluxbus-local-minio
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - fluxbus-local-minio-data:/data
    networks:
      - fluxbus-local-network
    command: server /data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3
    restart: unless-stopped

  # ==========================================
  # WHATSAPP SERVICE - Local
  # ==========================================
  whatsapp:
    build:
      context: ./whatsapp-service
      dockerfile: Dockerfile
    image: fluxbus-whatsapp:local
    container_name: fluxbus-local-whatsapp
    hostname: fluxbus-local-whatsapp
    environment:
      PORT: 3333
      INSTANCE_KEY: fluxbus_db_local
      SESSION_DIR: /app/sessions
      NODE_ENV: development
    volumes:
      - fluxbus-local-whatsapp-sessions:/app/sessions
      - ./backend/holerites:/app/holerites:rw
    ports:
      - "3333:3333"
    networks:
      - fluxbus-local-network
    dns:
      - 8.8.8.8
      - 8.8.4.4
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3333/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped

  # ==========================================
  # BACKEND - Spring Boot Local (Hot Reload)
  # ==========================================
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.local
    image: fluxbus-backend:local
    container_name: fluxbus-local-backend
    hostname: fluxbus-local-backend
    environment:
      SPRING_PROFILES_ACTIVE: local
      SPRING_DATASOURCE_URL: jdbc:postgresql://fluxbus-local-db:5432/fluxbus_db_local
      SPRING_DATASOURCE_USERNAME: dev_user
      SPRING_DATASOURCE_PASSWORD: dev_pass
      SPRING_REDIS_HOST: fluxbus-local-redis
      SPRING_REDIS_PORT: 6379
      BAILEYS_ENABLED: "true"
      BAILEYS_REST_URL: http://fluxbus-local-whatsapp:3333
      JWT_SECRET: dev_secret_key_min_64_chars_for_hmac512_algorithm_local_only
      MINIO_URL: http://fluxbus-local-minio:9000
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
    ports:
      - "8083:8083"
      - "5005:5005"
    volumes:
      - ./backend/src:/app/src:ro
      - ./backend/target:/app/target
      - ./backend/holerites:/app/holerites:rw
      - fluxbus-local-backend-m2:/root/.m2
    networks:
      - fluxbus-local-network
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      whatsapp:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8083/api/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    restart: unless-stopped

  # ==========================================
  # FRONTEND - React Local (Hot Reload)
  # ==========================================
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.local
    image: fluxbus-frontend:local
    container_name: fluxbus-local-frontend
    hostname: fluxbus-local-frontend
    environment:
      VITE_API_URL: http://localhost:8083
      VITE_ENV: local
    ports:
      - "3000:3000"
    volumes:
      - ./frontend/src:/app/src:ro
      - ./frontend/public:/app/public:ro
      - fluxbus-local-frontend-modules:/app/node_modules
    networks:
      - fluxbus-local-network
    depends_on:
      - backend
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    restart: unless-stopped
    stdin_open: true
    tty: true

# ============================================
# VOLUMES
# ============================================
volumes:
  fluxbus-local-db-data:
    name: fluxbus-local-db-data
  fluxbus-local-redis-data:
    name: fluxbus-local-redis-data
  fluxbus-local-minio-data:
    name: fluxbus-local-minio-data
  fluxbus-local-whatsapp-sessions:
    name: fluxbus-local-whatsapp-sessions
  fluxbus-local-backend-m2:
    name: fluxbus-local-backend-m2
  fluxbus-local-frontend-modules:
    name: fluxbus-local-frontend-modules

# ============================================
# NETWORKS
# ============================================
networks:
  fluxbus-local-network:
    name: fluxbus-local-network
    driver: bridge





























