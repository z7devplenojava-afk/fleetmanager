# API via proxy do Vite → backend em http://localhost:8083 (ver vite.config.ts)
VITE_API_URL=/api
VITE_WS_URL=/ws
# Só use HTTPS no dev se precisar: VITE_DEV_HTTPS=true (cert autoassinado; por IP pode dar ERR_CERT_AUTHORITY_INVALID)
# VITE_DEV_HTTPS=true
