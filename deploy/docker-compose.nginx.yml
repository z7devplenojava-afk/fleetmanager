version: '3.8'

services:
  # Nginx - Proxy Reverso para todos os ambientes
  nginx:
    image: nginx:alpine
    container_name: fluxbus-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx-multi-env.conf:/etc/nginx/nginx.conf
      - ./nginx/ssl:/etc/nginx/ssl
      - ./nginx/logs:/var/log/nginx
    depends_on:
      - frontend-prod
      - frontend-dev
      - frontend-ci
      - frontend-test
    networks:
      - fluxbus-network
    restart: unless-stopped

networks:
  fluxbus-network:
    external: true
    name: fluxbus
